/**
 * @file
 * @description Route level middleware for authorization
 */
'use strict';

const debug = require('debug')('app:tbd');

module.exports = (req, res, next) => {
  debug('"app/routes/middlewares/authorize.js" to be implemented');
  next();
};
