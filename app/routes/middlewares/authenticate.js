/**
 * @file
 * @description Route level middleware for authentication
 */
'use strict';

const debug = require('debug')('app:tbd');

module.exports = (req, res, next) => {
  debug('"app/routes/middlewares/authenticate.js" to be implemented');
  next();
};
