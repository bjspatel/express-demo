/**
 * @file
 * @description Route level middleware for validation
 */
'use strict';

const debug = require('debug')('app:tbd');

module.exports = (req, res, next) => {
  debug('"app/routes/middlewares/validate.js" to be implemented');
  next();
};
